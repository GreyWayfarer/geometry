#include <stdio.h>

int main()
{
    FILE *file;
    struct data {
        struct triangle {
        };
        struct circle {
        };
        char name[10] 
    };
    
    file = fopen("fscanf.txt", "r");
    
    while (name[i] = fgetc(file) != EOF) {
        if (name[i] == "triangle"

   //while(fscanf (file, "%d%*c%d%*c%d\n", &(data.x), &(data.y), &(data.z)));
        
